#pragma once

#include "BArray.h"
#include <stddef.h>

template<typename T>
class PriorityQueue
{
    public:

        void enqueue(int priority, T item)
        {

        }

        T dequeue()
        {

        }

        size_t size()
        {
            return -1;
        }

    private:
};
